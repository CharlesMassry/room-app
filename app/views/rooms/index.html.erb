
<h1>
  Rooms
</h1>

<% if current_user.signed_in? %>
  <%= link_to "New Room", new_room_path %>
<% end %>
<ul>
  <% @rooms.each do |room| %>
    <li>
      <%= link_to room.name, room %>
    </li>
  <% end %>
</ul>
